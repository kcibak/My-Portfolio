---
type Props = {
	title: string;
	description: string;
};

const {
	title,
	description
} = Astro.props as Props;
---

<div class="card project-feature-card">
	<h3 class="heading-tight">{title}</h3>
	<p class="muted-copy">{description}</p>
</div>

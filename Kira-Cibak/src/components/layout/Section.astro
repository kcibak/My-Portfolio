---
const {
	as = 'section',
	padding = 'default',
	class: className,
	style
} = Astro.props;

const Tag = as;
---

<Tag class:list={['layout-section', className]} data-padding={padding} style={style}>
	<slot />
</Tag>

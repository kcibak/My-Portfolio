---
const {
	as = 'div',
	size = 'default',
	id,
	class: className,
	style
} = Astro.props;

const Tag = as;
---

<Tag id={id} class:list={['layout-container', className]} data-size={size} style={style}>
	<slot />
</Tag>

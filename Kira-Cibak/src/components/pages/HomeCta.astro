<!-- ---
import Button from '../ui/Button.astro';
---

<div class="cta-inline">
	<div class="cta-copy">
		<p class="cta-eyebrow">Need momentum?</p>
		<h2 class="cta-title">I help teams sharpen the product and ship the next move.</h2>
	</div>
	<div class="cta-actions">
		<Button variant="accent" href="/contact">Start a conversation</Button>
		<Button variant="secondary" href="/skills">View skills</Button>
	</div>
</div>

<style>
	.cta-inline {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		justify-content: space-between;
		gap: 1rem 1.5rem;
	}

	.cta-copy {
		display: grid;
		gap: 0.2rem;
		max-width: 34rem;
	}

	.cta-eyebrow {
		margin: 0;
		font-size: 0.82rem;
		font-weight: 700;
		letter-spacing: 0.18em;
		text-transform: uppercase;
		color: var(--color-accent);
	}

	.cta-title {
		margin: 0;
		font-size: clamp(1.35rem, 2vw, 1.9rem);
		line-height: 1.12;
		letter-spacing: -0.03em;
		text-wrap: balance;
	}

	.cta-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
	}

	@media (max-width: 720px) {
		.cta-inline {
			align-items: flex-start;
		}

		.cta-actions {
			width: 100%;
		}

		.cta-actions :global(.button) {
			flex: 1 1 12rem;
		}
	}
</style> -->
